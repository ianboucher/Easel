%br/

.img-responsive
  - @bio.images.each do |img|
    = image_tag(img.url, class: 'preview-large',
      style: "position:absolute; top:#{img.posY}px; left: #{img.posX}px")
%h1= @bio.name
%h3= @bio.description
%p= @bio.statement
%p= @bio.info
= link_to "Edit your Bio", edit_bio_path, class: 'btn btn-primary'
