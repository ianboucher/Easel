%br/

.img-responsive
  - @bio.images.each do |img|
    = image_tag(img.url, class: 'preview-large', top: 0, left: 0)
%h1= @bio.name
%h3= @bio.description
%p= @bio.statement
%p= @bio.info
= link_to "Edit your Bio", edit_bio_path, class: 'btn btn-primary'
